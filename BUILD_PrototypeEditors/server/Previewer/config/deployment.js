'use strict';

// Deployment configuration
// ===========================

module.exports = {
    //deploymentPath: (process.env.SHARED_FOLDER || './../../../norman-deploy-and-share/public/')
    deploymentPath: (process.env.SHARED_FOLDER || './../../../')
};
